# Disable PHP execution under data/ (covers, score files, avatars).
# Avoid Apache php_* flags here — on some hosts they make the whole directory return HTTP 500.
<FilesMatch "\.php$">
    Require all denied
</FilesMatch>
