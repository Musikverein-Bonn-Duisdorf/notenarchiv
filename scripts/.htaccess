# CLI tools only — never serve PHP (or anything) from /scripts/ over HTTP.
# Belt-and-suspenders with root RedirectMatch 403 /scripts/
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
